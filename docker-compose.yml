services:
  api1: &api
    build: .
    image: rinha-2026-go-api:latest
    networks:
      - rinha
    # Per-replica budget. The reference dataset is shipped as an mmap'd
    # quantized blob, so the Go heap itself stays small and we bound it via
    # GOMEMLIMIT below.
    environment:
      GOGC: "50"
      GOMEMLIMIT: "80MiB"
      GOMAXPROCS: "1"
      SEARCH_NPROBE: "12"
      SEARCH_MAX_NPROBE: "24"
      SEARCH_ADAPTIVE: "true"
    # Top-level cpus / mem_limit are honored by `docker compose up` without
    # swarm; the deploy block keeps the same caps for tooling that reads it
    # (contest runner, swarm).
    cpus: 0.40
    mem_limit: 160m
    deploy:
      resources:
        limits:
          cpus: "0.40"
          memory: "160MB"

  api2:
    <<: *api

  lb:
    image: nginx:1.27-alpine
    ports:
      - "9999:9999"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - api1
      - api2
    networks:
      - rinha
    cpus: 0.20
    mem_limit: 30m
    deploy:
      resources:
        limits:
          cpus: "0.20"
          memory: "30MB"

networks:
  rinha:
    driver: bridge
